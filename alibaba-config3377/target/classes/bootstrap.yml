server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.56.128:8848  #Nacos服务注册中心地址
      config:
        server-addr: 192.168.56.128:8848  #Nacos作为配置中心地址
        file-extension: yml #指定选择拉取yaml格式的配置
#        group: "SJ_GROUP" #设置组信息，不设置默认是DEFAULT_GROUP
#        namespace: ebb54b8d-f01f-40ba-8f7e-bf3b862176e0 #配置命令空间，不设置默认是PUBLIC



