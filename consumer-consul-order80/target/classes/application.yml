server:
  port: 80
spring:
  application:
    name: cloud-consumer-consul-order
  cloud:
    consul:
      # consul注册中心地址
      host: 192.168.0.44
      port: 8500
      discovery:
        hostname: 192.168.0.233
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true